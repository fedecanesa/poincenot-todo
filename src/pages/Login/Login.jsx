import React from 'react';
import PropTypes from 'prop-types';
import './login.scss';

const Login = props => {
    return (
        <div>Login</div>
    )
}

Login.propTypes = {}

export default Login;