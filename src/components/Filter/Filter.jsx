import React from 'react';
import PropTypes from 'prop-types';
import './filter.scss';

const Filter = props => {
    return (
        <div>Filter</div>
    )
}

Filter.propTypes = {}

export default Filter;