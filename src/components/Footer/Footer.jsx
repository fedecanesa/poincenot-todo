import React from 'react';
import PropTypes from 'prop-types';
import './footer.scss';

const Footer = props => {
    return (
        <div>Footer</div>
    )
}

Footer.propTypes = {}

export default Footer