import React from 'react';
import PropTypes from 'prop-types';
import './input.scss';

const Input = props => {
    return (
        <div>Input</div>
    )
}

Input.propTypes = {}

export default Input;