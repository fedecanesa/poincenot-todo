import React from 'react';
import PropTypes from 'prop-types';
import './layout.scss';

const Layout = props => {
    return (
        <div>Layout</div>
    )
}

Layout.propTypes = {}

export default Layout;